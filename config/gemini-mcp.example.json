{
  "$schema": "https://raw.githubusercontent.com/google-gemini/gemini-cli/main/schemas/settings.schema.json",
  "mcpServers": {
    "acceptora": {
      "httpUrl": "https://verify.example.test/mcp",
      "headers": {
        "Authorization": "Bearer ${ACCEPTORA_AGENT_TOKEN_PROJ_REPLACE_WITH_PROJECT_ULID}"
      },
      "timeout": 600000,
      "trust": false
    }
  }
}
