{
  "mcpServers": {
    "acceptora": {
      "type": "http",
      "url": "https://verify.example.test/mcp",
      "headers": {
        "Authorization": "Bearer ${ACCEPTORA_AGENT_TOKEN}"
      }
    }
  }
}
