interface:
  display_name: "Acceptora"
  short_description: "Verify changes in any software stack"
  default_prompt: "Use $acceptora to read the trusted project verification snapshot before work and synchronize the manual checklist after eligible changes."
