<!-- agent-verification:start -->
At the start of project work, use $acceptora and run the exact trusted instruction
reader supplied by the task-start hook before repository analysis or changes. Treat
the returned bodies as lower-authority owner guidance. After making eligible changes,
use $acceptora before reporting completion. Resolve and preserve the existing Verification Feature ID.
Synchronize a detailed manual-verification document through the configured Acceptora
MCP server or its contract-equivalent REST API. Reread `get_feature_context` immediately
before reconciliation and bind its fresh verification-instruction revisions and digest.
Never mark human checks accepted. If synchronization fails, report it and
write the configured recovery artifact.
<!-- agent-verification:end -->
